import { Constant } from "types";

const userTypes: Constant = {
  SUPPLIER: "supplier",
  TRADER: "trader",
  TRANSPORTER: "transporter",
};

const supportedLanguages: Constant = {
  AR: "ar",
  EN: "en",
};

export { userTypes, supportedLanguages };
